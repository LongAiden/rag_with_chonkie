# Utility Scripts

There are currently no standalone utility scripts required for the RAG + Knowledge Graph workflow.
Entity and relationship extraction runs automatically as part of the document upload pipeline, so
the previous `extract_existing_chunks.py` migration script has been removed. If new automation
scripts are introduced in the future, document them here.
