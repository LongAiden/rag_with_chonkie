# Core ML Stack - Version Pinned for Stability
numpy>=2.0.0,<3.0  # Updated for chonkie compatibility
scipy>=1.10.0,<1.15.0
# CPU wheels avoid downloading large CUDA artifacts during image builds
torch>=2.3.1,<2.6.0
sentence-transformers>=3.0.0  # Required by chonkie >=1.4.0
huggingface-hub>=0.19.0,<1.0.0  # Pin compatible version

# Vector Database & SQL
psycopg2-binary>=2.9.0
pgvector>=0.3.0
sqlalchemy[asyncio]>=2.0.0
greenlet>=2.0.0
asyncpg # Replace pscycop2

# Document Processing Pipeline
pypdf>=3.17.0
python-docx>=1.0.0
docx2txt>=0.8
tiktoken>=0.5.2
PyPDF2

# Chunking Strategy
chonkie>=1.4.0  # Latest stable version (1.4.2) with bug fixes

# LLM Integration
google-generativeai>=0.8.0

# ReRank
rank_bm25

# Graph & Knowledge Extraction
spacy>=3.7.0  # For NER and entity extraction
spacy-transformers>=1.3.0  # Transformer-based models for spacy
networkx>=3.0  # For graph analysis and algorithms
# Note: pgRouting is a PostgreSQL extension, not a Python package

# Web Interface & API
streamlit>=1.31.0
fastapi>=0.104.0
pydantic>=2.5.0
pydantic-settings>=2.0.0  # For configuration management
pydantic-ai>=0.0.7
pydantic-ai-slim[google]
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Environment Management
python-dotenv>=1.0.0

# Production Essentials (Add these)
gunicorn>=21.2.0  # WSGI server for production
logfire # For logging

# Async task queue
celery[redis]>=5.3.0
redis>=5.0.0
